<source>
  type forward
  port 24224
</source>

<match test.**>
  type copy
  <store>
    type null
  </store>
  <store>
    type flowcounter
    unit minute
    aggregate all
    tag bench.messages
    count_keys message
  </store>
</match>

<match bench.**>
  type stdout
</match>
